// import { createAction, handleActions } from "redux-actions"

// const initialState = {
//     value: 0
// }


// const prefix = "project/counter/"
// export const INCREMENT = `${prefix}INCREMENT`
// export const DECREMENT = `${prefix}DECREMENT`

// export const increment = createAction(INCREMENT)
// export const decrement = createAction(DECREMENT)


// const reducer = handleActions(
//     {
//         [INCREMENT]: (state :object, action) => ({
//             // 이 경우에는 payload가 없으므로 action을 생략해도 무관하다.
//             value: state.value + 1
//         }),
//         [DECREMENT]: (state :object, action) => ({
//             value: state.value - 1
//         })
//     },
//     initialState
// )

export default {}